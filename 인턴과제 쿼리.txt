DROP DATABASE IF EXISTS phpboard;
CREATE DATABASE phpboard;
USE phpboard;
CREATE TABLE article (
    id INT UNSIGNED NOT NULL AUTO_INCREMENT,
    PRIMARY KEY(id),
    createDate DATETIME NOT NULL,
    modifyDate DATETIME,
    `title` VARCHAR(100) NOT NULL,
    `body` TEXT NOT NULL,
    `author` VARCHAR(50) NOT NULL,
    `hit` INT(10) UNSIGNED DEFAULT 0
);
SHOW TABLES;
DESC article;
SELECT * FROM article;
INSERT INTO article SET title = '제목1', `body` = '내용1', createDate = NOW(), author = '황인성';
INSERT INTO article SET title = '제목2', `body` = '내용2', createDate = NOW(), author = '관리자';
SHOW TABLES;